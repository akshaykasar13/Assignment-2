# === PostgreSQL ===
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/cicd

# === CORS / Frontend ===
FRONTEND_ORIGIN=http://localhost:5173

# === Alerts (Gmail SMTP) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
ALERT_TO=recipient@gmail.com

# === Metrics Window (minutes) ===
METRICS_DEFAULT_WINDOW=1440
